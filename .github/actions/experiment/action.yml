
name: 'experiment'
       
runs:
  using: "composite"
  steps:
    - shell: bash
      id: return_info
      run: |
        TEST_VARIABLE="trying it out"
        echo "set output"
        echo "test: ${ TEST_VARIABLE }"
        
