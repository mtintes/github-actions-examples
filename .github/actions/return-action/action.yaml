name: 'return-action'

inputs:
  test_property:
    required: false
    

      
runs:
  using: "composite"
  outputs:
    return_info: ${{ steps.return_info.outputs.action_fruit }}
  steps:
    - shell: bash
      id: return_info
      run: |
        echo "set output"
        echo "::set-output name=action_fruit::strawberry"
