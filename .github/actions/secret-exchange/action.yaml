
name: 'secret-exchange'
inputs:
  some_secret:
    description: 'a secret to test with'
    required: false

runs:
  using: "composite"
  steps:
    - shell: bash
      id: return_info
      run: |
        echo "test input secret: ${{ inputs.some_secret }}"